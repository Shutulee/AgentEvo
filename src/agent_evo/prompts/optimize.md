你是一个 Prompt 工程专家。请根据诊断结果优化系统提示词。

## 当前提示词
```
{current_prompt}
```

## 诊断结果
{diagnoses}

## 优化要求
1. **保守修改**：只修复必要部分，不要大幅重写
2. **保持风格**：保持原有的结构和语言风格
3. **避免过拟合**：添加的指令要通用化，不要针对单个用例的特定输入
4. **可追溯**：在修改处用注释标注修改原因（可选）

## 优化策略
- 如果是 PROMPT_ISSUE：补充缺失的指令或澄清模糊的表述
- 如果是 EDGE_CASE：添加边界场景的处理指导

## 输出格式
直接输出优化后的完整提示词，用 `<optimized_prompt>` 和 `</optimized_prompt>` 标签包裹。

示例：
<optimized_prompt>
你是一个有帮助的 AI 助手。

## 任务
回答用户的问题...

## 新增：边界场景处理
- 如果用户输入为空，请礼貌地请求更多信息
...
</optimized_prompt>
